[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
cache_dir = ".cache/pytest"

[tool.ruff]
target-version = "py314"
line-length = 110
exclude = ["python", "vendor", ".cache"]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

